---
phase: 01-foundation-auth-and-offline-core
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/app/page.tsx, src/app/api/auth/session/route.ts, src/features/auth/**/*, src/features/offline/**/*, src/lib/crypto/device-crypto.ts, src/lib/offline/db.ts, src/lib/offline/sync-queue.ts, src/lib/session/offline-session.ts, src/lib/session/policy.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, SYNC-01, SYNC-02]
user_setup: []
must_haves:
  truths:
    - "AUTH-02, D-01, and D-02: A user who has logged in online before can log in again while offline from an encrypted local credential cache, while a first-time user cannot."
    - "D-05, D-06, and D-07: The app restores a valid local session immediately, expires idle sessions after 6 hours, and forces Monday re-login even when offline."
    - "AUTH-03, SYNC-01, SYNC-02, D-08, D-09, D-10, and D-11: Local session data and the append-only sync queue persist safely for 7 days offline and track retry metadata plus delta payloads for later sync."
    - "D-03 and D-04: When the server reports passwordVersion or role changes after reconnect, the current user is forced to sign in again."
  artifacts:
    - path: "src/lib/offline/db.ts"
      provides: "Dexie schema for credential cache, local session records, app metadata, and sync_queue"
      contains: "sync_queue"
    - path: "src/lib/session/policy.ts"
      provides: "Idle timeout, Monday re-auth, and reconnect invalidation rules"
      contains: "6 * 60 * 60"
    - path: "src/lib/crypto/device-crypto.ts"
      provides: "Web Crypto helpers for local credential encryption and verification"
      contains: "crypto.subtle"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/session/offline-session.ts"
      via: "offline login and restore flow"
      pattern: "offline-session"
    - from: "src/lib/offline/sync-queue.ts"
      to: "src/lib/offline/db.ts"
      via: "Dexie table access"
      pattern: "sync_queue"
    - from: "src/app/api/auth/session/route.ts"
      to: "src/lib/session/policy.ts"
      via: "passwordVersion and role invalidation check"
      pattern: "passwordVersion|role"
---

<objective>
Extend the Walking Skeleton into a local-first auth and persistence slice: authenticated users can re-enter the app offline on the same device, session rules are enforced locally, and the baseline sync queue exists for future server sync work.

Purpose: This plan translates the phase's hardest offline auth decisions into concrete code before the more visual shell and PWA work begins.
Output: Dexie local database schema, encrypted credential cache, local session policy logic, and reconnect invalidation hooks.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth-and-offline-core/01-CONTEXT.md
@.planning/phases/01-foundation-auth-and-offline-core/01-UI-SPEC.md
@.planning/phases/01-foundation-auth-and-offline-core/SKELETON.md
@.planning/phases/01-foundation-auth-and-offline-core/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Dexie local data model and encrypted credential envelope</name>
  <files>src/lib/offline/db.ts, src/lib/offline/sync-queue.ts, src/lib/crypto/device-crypto.ts</files>
  <read_first>.planning/phases/01-foundation-auth-and-offline-core/01-CONTEXT.md, .planning/phases/01-foundation-auth-and-offline-core/SKELETON.md, src/lib/db/prisma.ts, src/lib/auth/password.ts</read_first>
  <action>Create a single Dexie instance that owns `credential_cache`, `local_sessions`, `sync_queue`, and `app_meta` tables. The `sync_queue` table must be append-only and include `status`, `attemptCount`, `nextRetryAt`, `entityType`, `entityId`, `deltaPayload`, `allowNegativeStock`, and `createdAt`. Implement `src/lib/crypto/device-crypto.ts` with Web Crypto AES-GCM and PBKDF2 helpers so an online login can persist a local credential envelope without storing a plain password. Use a per-device salt in `app_meta` and keep the envelope keyed by username plus passwordVersion so reconnect invalidation can compare versions later.</action>
  <verify>pnpm lint && pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - src/lib/offline/db.ts exports one Dexie instance and declares tables for `credential_cache`, `local_sessions`, `sync_queue`, and `app_meta`
    - src/lib/offline/sync-queue.ts defines the only allowed queue statuses as `pending`, `sent`, `acked`, and `failed`
    - src/lib/crypto/device-crypto.ts contains `crypto.subtle` calls for PBKDF2 and AES-GCM
    - sync_queue records include retry metadata (`attemptCount` and `nextRetryAt`) plus `allowNegativeStock`
  </acceptance_criteria>
  <done>The local persistence layer exists with encrypted credential support and a sync queue contract that later phases can extend.</done>
</task>

<task type="auto">
  <name>Task 2: Wire offline login, restore, Monday re-auth, and secure logout</name>
  <files>src/app/page.tsx, src/features/auth/**/*, src/lib/session/offline-session.ts, src/lib/session/policy.ts</files>
  <read_first>src/app/page.tsx, .planning/phases/01-foundation-auth-and-offline-core/01-CONTEXT.md, .planning/phases/01-foundation-auth-and-offline-core/01-01-SUMMARY.md, src/lib/crypto/device-crypto.ts, src/lib/offline/db.ts, src/lib/auth/server-session.ts</read_first>
  <action>Update the login entry flow so a successful online login writes both the encrypted credential envelope and a local session snapshot. Implement offline login only for usernames that already have a cached credential envelope: derive the verifier from the submitted password, compare it to the cached value, and deny unknown users while offline. Add session policy helpers that restore a valid local session immediately, invalidate sessions idle for more than 6 hours, and force Monday re-login based on Asia/Jakarta time even when the device is offline. Implement logout so it clears the local session record, removes any cached server session cookie state, and leaves no reusable active session on the device.</action>
  <verify>pnpm lint && pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - src/app/page.tsx contains an explicit offline branch that rejects first-time users while offline
    - src/lib/session/policy.ts contains a 6-hour idle timeout rule and a Monday re-auth rule tied to Asia/Jakarta
    - src/lib/session/offline-session.ts exports helpers to persist, restore, and clear the local session snapshot
    - logout flow clears the local session record and does not leave an active offline session behind
  </acceptance_criteria>
  <done>Returning users can re-enter the app offline on the same device while the required idle and Monday session rules are enforced locally.</done>
</task>

<task type="auto">
  <name>Task 3: Add reconnect invalidation and baseline sync retry behavior</name>
  <files>src/app/api/auth/session/route.ts, src/features/offline/**/*, src/lib/offline/sync-queue.ts, src/lib/session/policy.ts</files>
  <read_first>src/app/api/auth/session/route.ts, src/lib/offline/db.ts, src/lib/offline/sync-queue.ts, .planning/phases/01-foundation-auth-and-offline-core/01-CONTEXT.md</read_first>
  <action>Extend the session route and offline services so reconnect checks compare the server-side `passwordVersion` and role against the locally cached values. If either differs for the currently signed-in user, clear the local session and require a fresh login. In the same slice, implement queue helpers for immediate retry followed by exponential backoff, persist `lastAttemptAt`, and keep queue/session records for a minimum 7-day offline window before any cleanup. The queue contract must remain delta-based and retain `allowNegativeStock` metadata without implementing stock conflict resolution yet.</action>
  <verify>pnpm lint && pnpm exec tsc --noEmit</verify>
  <acceptance_criteria>
    - src/app/api/auth/session/route.ts compares passwordVersion or role and returns an invalidation signal when the local cache is stale
    - src/lib/offline/sync-queue.ts schedules immediate first retry and exponential backoff for later retries
    - local cleanup logic preserves queue and session data for at least 7 days of offline operation
    - queue payload helpers retain delta payload and `allowNegativeStock` metadata without collapsing entries in place
  </acceptance_criteria>
  <done>Offline auth state stays aligned with server credential changes and the sync queue preserves enough metadata for Phase 2 sync work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser-storage->offline-auth | Encrypted credential and session data live on a user-controlled device. |
| online-session->offline-session | Server-authenticated state is converted into a local offline credential envelope and restored later. |
| reconnect-check->session-validity | Reconnected devices reconcile local credential versions against server-side truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | src/lib/crypto/device-crypto.ts | mitigate | Store only encrypted credential envelopes plus derived verifiers; never persist a plaintext password or reusable secret. |
| T-01-05 | Elevation of Privilege | src/lib/session/policy.ts | mitigate | Force re-login when server role or passwordVersion changes, and expire idle sessions after 6 hours plus Monday re-auth. |
| T-01-06 | Denial of Service | src/lib/offline/sync-queue.ts | mitigate | Use immediate retry followed by exponential backoff and persisted attempt metadata to prevent retry storms on reconnect. |
| T-01-SC | Tampering | npm package installs | mitigate | Reuse the approved package list from Plan 01; do not add new runtime packages in this plan without another legitimacy checkpoint. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] pnpm lint succeeds
- [ ] pnpm exec tsc --noEmit succeeds
- [ ] Returning users can sign in while offline after one successful online login
- [ ] Changing passwordVersion or role on reconnect invalidates the current local session
</verification>

<success_criteria>

- All tasks completed
- Returning users can log back in offline on the same device while first-time users still require online auth
- Session restore, idle timeout, Monday re-auth, and logout match the locked phase decisions
- The local sync queue persists for 7 days and records the retry metadata needed for later sync execution
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth-and-offline-core/01-02-SUMMARY.md`
</output>
