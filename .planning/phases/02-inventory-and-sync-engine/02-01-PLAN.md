---
phase: 02-inventory-and-sync-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, src/lib/offline/db.ts, src/lib/inventory/mutation-event.ts, src/features/inventory/hooks/use-stock-mutation.ts, src/features/inventory/components/negative-stock-warning.tsx, src/features/inventory/components/stock-mutation-form.tsx, tests/inventory/mutation-queue.spec.ts, tests/sync/negative-stock-warning.spec.tsx]
autonomous: true
requirements: [INV-01, INV-03, SYNC-05]
must_haves:
  truths:
    - "Authorized user can save Stock In and Stock Adjustment while offline, and records are queued locally with required mutation metadata (D-01, D-02, D-03)."
    - "Cashier sees a soft warning before a cart action that causes/deepens negative stock, but can always bypass with Enter and continue (D-08, D-09, D-10, D-11)."
    - "Mutation events are persisted as item-granular deltas, not batched documents, remain separate from any POS transaction sync table, and are ready for sync queue processing (D-01, D-04, D-15)."
    - "Critical semantic sync errors such as missing product ids or invalid schema mappings must transition queue work to `failed` without auto-retry while preserving local event data for traceback (D-16)."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Inventory mutation and stock projection schema primitives for phase-2 event model"
      contains: "model InventoryMutationEvent"
    - path: "src/lib/offline/db.ts"
      provides: "Dexie tables/indexes for mutation events and queue records"
      contains: "inventoryMutationEvents"
    - path: "src/features/inventory/hooks/use-stock-mutation.ts"
      provides: "Offline-first stock in/adjust mutation builder and queue writer"
      exports: ["useStockMutation"]
    - path: "src/features/inventory/components/negative-stock-warning.tsx"
      provides: "Soft warning dialog copy and Enter-to-bypass behavior"
      contains: "Stok barang ini kurang, stock akhir akan 0 atau mines!"
  key_links:
    - from: "src/features/inventory/hooks/use-stock-mutation.ts"
      to: "src/lib/offline/db.ts"
      via: "Dexie transaction appending mutation + sync queue rows"
      pattern: "db\\.transaction"
    - from: "src/features/inventory/components/stock-mutation-form.tsx"
      to: "src/features/inventory/components/negative-stock-warning.tsx"
      via: "warning gate before committing negative stock action"
      pattern: "negative-stock-warning"
    - from: "tests/sync/negative-stock-warning.spec.tsx"
      to: "src/features/inventory/components/negative-stock-warning.tsx"
      via: "component behavior assertion"
      pattern: "Stok barang ini kurang"
---

<objective>
Deliver the first MVP vertical slice for inventory: a cashier can perform Stock In and Stock Adjustment offline with append-only mutation events and receive the required soft negative-stock warning behavior.

Purpose: This establishes the user-facing inventory mutation flow and local event contract that all sync reliability work builds on.
Output: Mutation schema/tables, offline mutation hook + form wiring, warning dialog behavior, and passing tests for local queue/event behavior.
</objective>

<execution_context>
@C:/Users/lukas/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lukas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md
@.planning/phases/02-inventory-and-sync-engine/02-RESEARCH.md
@.planning/phases/02-inventory-and-sync-engine/02-PATTERNS.md
@.planning/phases/02-inventory-and-sync-engine/02-UI-SPEC.md
@src/lib/offline/db.ts
@src/lib/offline/sync-queue.ts
@src/components/auth/login-form.tsx
@src/hooks/use-connectivity.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for offline stock in/adjust queueing and negative-stock warning</name>
  <files>tests/inventory/mutation-queue.spec.ts, tests/sync/negative-stock-warning.spec.tsx</files>
  <read_first>tests/inventory/mutation-queue.spec.ts, tests/sync/negative-stock-warning.spec.tsx, .planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md, .planning/phases/02-inventory-and-sync-engine/02-UI-SPEC.md, src/lib/offline/sync-queue.ts, src/components/auth/login-form.tsx</read_first>
  <behavior>
    - Test 1: Saving `STOCK_IN` appends one granular mutation event with required metadata fields per D-02 and enqueues a `pending` queue row per D-15.
    - Test 2: Saving `STOCK_ADJUSTMENT` appends one mutation event with enum from D-03 and preserves append-only behavior.
    - Test 3: Warning displays exact text `Stok barang ini kurang, stock akhir akan 0 atau mines!` and Enter confirms bypass per D-10 and D-11.
  </behavior>
  <action>Create failing tests that assert concrete event payload keys (`id_queue`, `id_transaksi`, `id_produk`, `id_user`, `jenis_mutasi`, `delta_qty`, `logical_clock`, `client_timestamp`) and queue status `pending` for new writes per D-02 and D-15. Use `tests/inventory/mutation-queue.spec.ts` assertions patterned after queue state expectations from analog `src/lib/offline/sync-queue.ts` (status/backoff/state transition vocabulary from 02-PATTERNS). Add component tests for warning visibility trigger when requested qty exceeds stock or stock `<= 0` per D-09, and for keyboard Enter bypass continuation per D-11, mirroring component error/warning assertion style in analog `src/components/auth/login-form.tsx` per 02-PATTERNS. Keep assertions on Indonesian copy and on non-blocking continuation semantics per D-08.</action>
  <verify>
    <automated>npm run test -- tests/inventory/mutation-queue.spec.ts tests/sync/negative-stock-warning.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both new test files exist and initially fail with at least one assertion error each
    - mutation queue test asserts all D-02 metadata keys by exact key names
    - warning test asserts exact D-10 message string and Enter key path
    - no production file is modified in this task
  </acceptance_criteria>
  <done>Failing tests define the expected inventory mutation and warning behavior before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement offline stock in/adjust slice with granular mutation events and soft warning dialog</name>
  <files>prisma/schema.prisma, src/lib/offline/db.ts, src/lib/inventory/mutation-event.ts, src/features/inventory/hooks/use-stock-mutation.ts, src/features/inventory/components/negative-stock-warning.tsx, src/features/inventory/components/stock-mutation-form.tsx</files>
  <read_first>prisma/schema.prisma, src/lib/offline/db.ts, src/lib/offline/sync-queue.ts, src/features/format/currency.ts, .planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md, .planning/phases/02-inventory-and-sync-engine/02-PATTERNS.md, .planning/phases/02-inventory-and-sync-engine/02-UI-SPEC.md</read_first>
  <behavior>
    - Behavior 1: Offline stock in/adjust form writes mutation events locally and immediately returns control to cashier flow (non-blocking).
    - Behavior 2: Each event is item-granular and carries D-02 metadata with `jenis_mutasi` constrained to D-03 values used in this slice.
    - Behavior 3: Negative stock warning is soft, uses exact D-10 copy, and Enter bypass allows continuation without approval.
  </behavior>
  <action>Extend Prisma and Dexie schemas with `InventoryMutationEvent` and required queue linkage so local-first writes are append-only per D-01 and metadata-complete per D-02; map schema/index style directly to analog `src/lib/offline/db.ts` (Dexie version/store declaration pattern from 02-PATTERNS). Implement `src/lib/inventory/mutation-event.ts` DTO/builder helpers for `STOCK_IN` and `STOCK_ADJUSTMENT` enum values per D-03, and add `useStockMutation` hook using hook lifecycle/callback structure from analog `src/hooks/use-connectivity.ts` plus queue write semantics from analog `src/lib/offline/sync-queue.ts` per 02-PATTERNS. Build `stock-mutation-form` and `negative-stock-warning` components using submit/error/warning interaction pattern from analog `src/components/auth/login-form.tsx` and 02-UI-SPEC spacing/color/copy contracts; warning trigger and bypass must implement D-08 to D-11 exactly.</action>
  <verify>
    <automated>npm run test -- tests/inventory/mutation-queue.spec.ts tests/sync/negative-stock-warning.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `model InventoryMutationEvent` and fields matching D-02 identifier names
    - src/lib/offline/db.ts defines `inventoryMutationEvents` table and queue linkage for `entityType: "inventory_mutation"`
    - src/lib/inventory/mutation-event.ts exports builders for `STOCK_IN` and `STOCK_ADJUSTMENT`
    - warning component contains exact D-10 copy string and Enter key handler path
    - both Task 1 test files pass
  </acceptance_criteria>
  <done>The first inventory slice works offline end-to-end with required warning behavior and append-only mutation records.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push Prisma schema and run phase-slice verification</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma, package.json, .planning/phases/02-inventory-and-sync-engine/02-01-PLAN.md</read_first>
  <action>After all schema edits in Task 2 are complete, run `npx prisma db push` so local database state matches `prisma/schema.prisma` before any verification sign-off. If Prisma requires destructive non-TTY confirmation, rerun with `npx prisma db push --accept-data-loss` and record that it was required. Then run targeted tests and lint for this slice.</action>
  <verify>
    <automated>npx prisma db push && npm run test -- tests/inventory/mutation-queue.spec.ts tests/sync/negative-stock-warning.spec.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Prisma db push command exits 0 after schema changes
    - local database schema reflects `InventoryMutationEvent` without pending drift
    - targeted tests and lint pass after db push
  </acceptance_criteria>
  <done>Schema and local DB are synchronized, preventing false-positive verification for phase 2 inventory slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->indexeddb | Untrusted form input is transformed into local mutation events and queue records. |
| client->future-sync-api | Local queued mutations will later cross into server ingestion boundaries. |
| dependency/runtime->schema | Prisma schema changes affect data integrity for mutation replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/lib/inventory/mutation-event.ts | mitigate | Validate mutation shape and enum values before persisting local event rows; reject missing D-02 fields. |
| T-02-02 | Elevation | src/features/inventory/hooks/use-stock-mutation.ts | mitigate | Enforce authenticated user identity (`id_user`) binding from active session context rather than raw input payload. |
| T-02-03 | Repudiation | prisma/schema.prisma / mutation ledger | mitigate | Use append-only mutation rows with immutable metadata for local forensic traceback per D-16. |
| T-02-SC | Tampering | npm/pnpm installs | mitigate | No new package installs in this plan; if added, run slopcheck gate and human verification before install. |
</threat_model>

<verification>
- `npx prisma db push` succeeds after schema edits
- `npm run test -- tests/inventory/mutation-queue.spec.ts tests/sync/negative-stock-warning.spec.tsx` passes
- `npm run lint` passes
</verification>

<success_criteria>
- INV-01 and INV-03 are demonstrably usable offline via stock mutation form
- SYNC-05 warning behavior matches D-08..D-11 exactly
- Local mutation event data contract for D-01..D-03 is implemented and test-covered
</success_criteria>

<output>
Create `.planning/phases/02-inventory-and-sync-engine/02-01-SUMMARY.md` when done
</output>
