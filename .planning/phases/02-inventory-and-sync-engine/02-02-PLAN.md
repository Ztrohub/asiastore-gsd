---
phase: 02-inventory-and-sync-engine
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/lib/offline/inventory-sync.ts, src/app/api/sync/inventory-deltas/route.ts, src/lib/db/inventory-replay.ts, src/lib/inventory/mutation-event.ts, src/features/pos/hooks/use-stock-out-mutation.ts, tests/sync/reconnect-backoff.spec.ts, tests/sync/server-replay-order.spec.ts]
autonomous: true
requirements: [INV-02, INV-04, SYNC-03, SYNC-04, SYNC-06]
must_haves:
  truths:
    - "Stock-out mutation from POS is captured as `SALES_OUT` delta event, while POS transaction synchronization remains in a separate table from inventory mutation events, and deltas sync automatically after reconnection (D-03, D-04, D-13, D-14)."
    - "Sync processing is non-blocking for cashier flow and runs in background with periodic and online-triggered cycles (D-12, D-13)."
    - "Server applies delta events deterministically using FIFO receive order and client timestamp tie-break, allowing negative results (D-05, D-06, D-07)."
    - "Queue retries use exponential backoff up to 5 attempts, critical semantic errors move work to `failed` without auto-retry while preserving local traceback data, and unstable queues expose a manual retry path (D-16, D-17, D-18)."
  artifacts:
    - path: "src/lib/offline/inventory-sync.ts"
      provides: "Background push/pull scheduler and retry state machine for inventory events"
      exports: ["startInventorySyncLoop", "retryInventorySyncNow"]
    - path: "src/app/api/sync/inventory-deltas/route.ts"
      provides: "Authenticated sync ingest endpoint with per-event ack/failure contract"
      exports: ["POST"]
    - path: "src/lib/db/inventory-replay.ts"
      provides: "Deterministic replay ordering and sequential stock projection apply"
      exports: ["applyInventoryDeltaBatch"]
  key_links:
    - from: "src/lib/offline/inventory-sync.ts"
      to: "/api/sync/inventory-deltas"
      via: "background POST push transport"
      pattern: "/api/sync/inventory-deltas"
    - from: "src/app/api/sync/inventory-deltas/route.ts"
      to: "src/lib/db/inventory-replay.ts"
      via: "ordered replay function call"
      pattern: "applyInventoryDeltaBatch"
    - from: "src/lib/db/inventory-replay.ts"
      to: "prisma inventory projection model"
      via: "sequential transactional delta apply"
      pattern: "prisma\\.\\$transaction"
---

<objective>
Deliver the second MVP vertical slice for sync reliability: capture POS stock-out deltas, run autonomous background sync, and apply deterministic server replay that stays consistent across 1-3 devices.

Purpose: This completes the offline-to-online inventory loop needed for robust daily operations and concurrency tolerance.
Output: Stock-out mutation hook, background sync scheduler, sync API endpoint, deterministic replay service, and passing sync reliability tests.
</objective>

<execution_context>
@C:/Users/lukas/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lukas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md
@.planning/phases/02-inventory-and-sync-engine/02-RESEARCH.md
@.planning/phases/02-inventory-and-sync-engine/02-PATTERNS.md
@.planning/phases/02-inventory-and-sync-engine/02-01-SUMMARY.md
@src/lib/offline/sync-queue.ts
@src/hooks/use-connectivity.ts
@src/app/api/auth/session/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing sync tests for reconnect auto-push, deterministic ordering, and retry exhaustion</name>
  <files>tests/sync/reconnect-backoff.spec.ts, tests/sync/server-replay-order.spec.ts</files>
  <read_first>tests/sync/reconnect-backoff.spec.ts, tests/sync/server-replay-order.spec.ts, .planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md, .planning/phases/02-inventory-and-sync-engine/02-RESEARCH.md, src/lib/offline/sync-queue.ts</read_first>
  <behavior>
    - Test 1: Pending inventory events auto-push after online recovery and periodic scheduler tick per D-13.
    - Test 2: Replay order is FIFO by server receive sequence, tie-broken by smallest `client_timestamp` per D-05 and D-06.
    - Test 3: Retry backoff caps at 5 attempts, marks unstable state, and exposes manual retry path per D-17 and D-18.
  </behavior>
  <action>Create failing tests for push loop and replay service using explicit fixture events for `SALES_OUT`, `STOCK_IN`, and `STOCK_ADJUSTMENT`. Build queue transition assertions (`pending -> sent -> acked` and critical `failed`) using analog transition vocabulary from `src/lib/offline/sync-queue.ts` per 02-PATTERNS. Assert ordering and final stock outcomes under concurrent simulated device batches for SYNC-06, including negative stock outcomes allowed by SYNC-04, with replay branch style aligned to the explicit guard/branch pattern in analog `src/app/api/auth/session/route.ts` from 02-PATTERNS.</action>
  <verify>
    <automated>npm run test -- tests/sync/reconnect-backoff.spec.ts tests/sync/server-replay-order.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both new sync test files exist and initially fail
    - replay test includes explicit assertions for D-05, D-06 ordering rules
    - reconnect test includes explicit 5-attempt cap assertion and manual retry trigger assertion
    - tests reference queue states `pending`, `sent`, `acked`, and `failed`
  </acceptance_criteria>
  <done>Failing tests lock deterministic sync and retry expectations before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stock-out capture, background sync loop, and deterministic server replay endpoint</name>
  <files>src/lib/offline/inventory-sync.ts, src/app/api/sync/inventory-deltas/route.ts, src/lib/db/inventory-replay.ts, src/lib/inventory/mutation-event.ts, src/features/pos/hooks/use-stock-out-mutation.ts</files>
  <read_first>src/lib/offline/inventory-sync.ts, src/lib/offline/sync-queue.ts, src/app/api/auth/session/route.ts, src/lib/db/prisma.ts, src/lib/inventory/mutation-event.ts, .planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md, .planning/phases/02-inventory-and-sync-engine/02-PATTERNS.md</read_first>
  <behavior>
    - Behavior 1: POS stock-out writes `SALES_OUT` delta events locally, separate from POS transaction sync table per D-04.
    - Behavior 2: Background sync loop runs decoupled from cashier flow, pushes/pulls periodically, and updates queue statuses per D-12 to D-15.
    - Behavior 3: Server endpoint applies ordered deltas sequentially and returns per-event ack/failed results, preserving negative stock allowance.
  </behavior>
  <action>Implement `use-stock-out-mutation` to enqueue `SALES_OUT` events with D-02 metadata and `jenis_mutasi=\"SALES_OUT\"` per D-03 while persisting POS transaction sync records separately per D-04, following local-first intent/write-before-network pattern from analog `src/components/auth/logout-button.tsx` in 02-PATTERNS. Build `inventory-sync.ts` scheduler using analog `src/hooks/use-connectivity.ts` lifecycle pattern (online/offline listeners + periodic timer) and analog `src/components/auth/logout-intent-flusher.tsx` flush-loop cleanup style; apply queue transitions/backoff semantics from analog `src/lib/offline/sync-queue.ts` for D-15 to D-17 and expose `retryInventorySyncNow` for D-18. Add `POST /api/sync/inventory-deltas` route using request/validation/JSON response structure from analog `src/app/api/auth/login/route.ts`, authenticate branch style from analog `src/app/api/auth/session/route.ts`, then call `applyInventoryDeltaBatch` implemented with shared Prisma client pattern from analog `src/lib/db/prisma.ts` and sequential `prisma.$transaction` replay for D-05 to D-07.</action>
  <verify>
    <automated>npm run test -- tests/sync/reconnect-backoff.spec.ts tests/sync/server-replay-order.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/features/pos/hooks/use-stock-out-mutation.ts creates `SALES_OUT` events with all D-02 metadata fields
    - src/lib/offline/inventory-sync.ts exports scheduler start and manual retry functions
    - src/app/api/sync/inventory-deltas/route.ts exports POST and returns per-event ack/failure payload
    - src/lib/db/inventory-replay.ts applies ordered deltas inside `prisma.$transaction` sequential loop
    - Task 1 sync tests pass
  </acceptance_criteria>
  <done>Inventory deltas flow from POS to local queue to deterministic server replay with resilient retry semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full phase-2 sync slice behavior with lint and targeted test suite</name>
  <files>tests/sync/reconnect-backoff.spec.ts, tests/sync/server-replay-order.spec.ts</files>
  <read_first>tests/sync/reconnect-backoff.spec.ts, tests/sync/server-replay-order.spec.ts, package.json, .planning/phases/02-inventory-and-sync-engine/02-02-PLAN.md</read_first>
  <action>Run the sync-targeted tests and lint to confirm INV-02, INV-04, SYNC-03, SYNC-04, and SYNC-06 coverage is stable. Ensure verification explicitly checks automatic reconnect sync, negative-stock-preserving delta apply, concurrency ordering determinism, and manual retry path after retry-cap exhaustion.</action>
  <verify>
    <automated>npm run test -- tests/sync/reconnect-backoff.spec.ts tests/sync/server-replay-order.spec.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - both sync test files pass in one command execution
    - lint exits 0
    - test output includes assertions covering D-05, D-06, D-07, D-17, and D-18 behaviors
  </acceptance_criteria>
  <done>Phase-2 sync engine slice is verifiably stable for reconnect and 1-3 device concurrency behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->sync-route | Untrusted queued events are sent over HTTP to sync ingestion endpoint. |
| sync-route->db-replay | API layer passes event batches into transactional replay logic. |
| multi-device->stock-projection | Concurrent device deltas can tamper with stock consistency if ordering is non-deterministic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | src/app/api/sync/inventory-deltas/route.ts | mitigate | Require authenticated session identity and reject unauthenticated sync POST requests. |
| T-02-05 | Tampering | src/lib/db/inventory-replay.ts | mitigate | Enforce deterministic ordering and idempotent queue-key checks before sequential apply. |
| T-02-06 | Denial | src/lib/offline/inventory-sync.ts | mitigate | Apply capped exponential backoff with unstable-state signaling and manual retry trigger. |
| T-02-SC | Tampering | npm/pnpm installs | mitigate | No new dependency install tasks in this plan; if introduced later, gate with package legitimacy verification. |
</threat_model>

<verification>
- `npm run test -- tests/sync/reconnect-backoff.spec.ts tests/sync/server-replay-order.spec.ts` passes
- `npm run lint` passes
- Sync endpoint returns ack/failed results matching queue state transitions
</verification>

<success_criteria>
- INV-02 and INV-04 are delivered as full stock-out + auto-sync behavior
- SYNC-03, SYNC-04, and SYNC-06 are covered by deterministic replay and reconnect retry tests
- Queue resilience and manual retry behavior align with D-15..D-18
</success_criteria>

<output>
Create `.planning/phases/02-inventory-and-sync-engine/02-02-SUMMARY.md` when done
</output>
