---
phase: 02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, prisma/seed.ts, src/lib/offline/db.ts, src/lib/offline/sync-queue.ts, src/lib/offline/inventory-sync.ts, src/lib/offline/inventory-sync-transport.ts, src/app/api/inventory/products/route.ts, src/lib/db/product-catalog.ts, src/features/inventory/hooks/use-product-catalog.ts, tests/inventory/product-catalog-sync.spec.ts]
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Inventory route cannot function offline unless product master data exists locally and can be updated without blocking on network connectivity."
    - "Tambah produk dan atur harga must persist integer IDR values only, with offline-first writes and later sync reconciliation rather than online-only forms."
    - "Product master synchronization must stay separate from inventory mutation events so phase-2 delta replay rules remain intact."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Product catalog persistence model with SKU, price, stock snapshot, and sync timestamps"
      contains: "model Product"
    - path: "src/lib/offline/db.ts"
      provides: "Dexie product cache and product-sync queue storage"
      contains: "products"
    - path: "src/features/inventory/hooks/use-product-catalog.ts"
      provides: "Local-first product list, create, update, and sync state access for inventory UI"
      exports: ["useProductCatalog"]
    - path: "src/app/api/inventory/products/route.ts"
      provides: "Authenticated product list/upsert endpoint for syncing local product changes"
      exports: ["GET", "POST"]
  key_links:
    - from: "src/features/inventory/hooks/use-product-catalog.ts"
      to: "src/lib/offline/db.ts"
      via: "Dexie reads/writes for cached products and pending product sync records"
      pattern: "offlineDb\\."
    - from: "src/lib/offline/inventory-sync.ts"
      to: "src/app/api/inventory/products/route.ts"
      via: "background push of queued product upserts"
      pattern: "/api/inventory/products"
    - from: "src/app/api/inventory/products/route.ts"
      to: "src/lib/db/product-catalog.ts"
      via: "server-side product persistence and list retrieval"
      pattern: "product-catalog"
---

<objective>
Deliver the product catalog foundation for the inventory UI: product records exist in both server persistence and local cache, can be created/updated offline-first, and sync independently from stock mutation events.

Purpose: This gives phase 02.1 a usable product source of truth before any inventory tab UI is built.
Output: Product schema, local product cache, product sync API/transport, and failing-then-passing tests for offline-first product catalog behavior.
</objective>

<execution_context>
@C:/Users/lukas/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lukas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md
@.planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md
@prisma/schema.prisma
@src/lib/offline/db.ts
@src/lib/offline/sync-queue.ts
@src/lib/offline/inventory-sync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for local-first product catalog create, price edit, and sync queue behavior</name>
  <files>tests/inventory/product-catalog-sync.spec.ts</files>
  <read_first>tests/inventory/mutation-queue.spec.ts, src/lib/offline/db.ts, src/lib/offline/sync-queue.ts, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md</read_first>
  <action>Create `tests/inventory/product-catalog-sync.spec.ts` with failing assertions for three contracts: creating a product writes a local product row plus a pending sync queue row, editing `harga_jual` keeps integer-only IDR values and updates future reads without mutating prior sync payload history, and queue rows for product upserts use a distinct `entityType` from `inventory_mutation`. Use exact fields `nama_produk`, `sku`, `harga_jual`, `stok_saat_ini`, and queue state `pending` so the test locks the data contract before implementation.</action>
  <verify>
    <automated>npm run test -- tests/inventory/product-catalog-sync.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/inventory/product-catalog-sync.spec.ts` exists and fails before implementation
    - the test asserts integer-only price storage and rejects decimal input
    - the test asserts product queue rows do not reuse `entityType: "inventory_mutation"`
    - no production file is modified in this task
  </acceptance_criteria>
  <done>Product catalog behavior is locked by tests before schema and sync implementation begins.</done>
</task>

<task type="auto">
  <name>Task 2: Implement product catalog schema, Dexie cache, and sync transport/api foundation</name>
  <files>prisma/schema.prisma, prisma/seed.ts, src/lib/offline/db.ts, src/lib/offline/sync-queue.ts, src/lib/offline/inventory-sync.ts, src/lib/offline/inventory-sync-transport.ts, src/app/api/inventory/products/route.ts, src/lib/db/product-catalog.ts, src/features/inventory/hooks/use-product-catalog.ts</files>
  <read_first>prisma/schema.prisma, prisma/seed.ts, src/lib/offline/db.ts, src/lib/offline/sync-queue.ts, src/lib/offline/inventory-sync.ts, src/app/api/sync/inventory-deltas/route.ts, src/lib/db/inventory-replay.ts, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md</read_first>
  <action>Add a `Product` persistence model with fields for product identity, optional SKU, integer selling price, current stock snapshot, active state, and sync timestamps; seed at least one owner-visible example product. Extend Dexie with a local `products` table and queue helpers for product upsert records using a distinct entity type such as `inventory_product`. Update the background sync loop and transport so product upserts can be pushed independently from inventory delta events. Implement authenticated `GET`/`POST /api/inventory/products` endpoints and a `useProductCatalog` hook that reads local cache first, writes local updates immediately, enforces integer-only prices between `100` and `999999999`, and schedules server reconciliation without blocking the UI.</action>
  <verify>
    <automated>npm run test -- tests/inventory/product-catalog-sync.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains a `Product` model with integer price storage
    - `src/lib/offline/db.ts` defines a `products` table plus local product sync storage
    - `src/features/inventory/hooks/use-product-catalog.ts` exports `useProductCatalog`
    - `src/app/api/inventory/products/route.ts` exports both `GET` and `POST`
    - the product catalog sync test passes
  </acceptance_criteria>
  <done>Product catalog data is available to the UI as a local-first, sync-capable source of truth.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push Prisma schema and verify product catalog foundation</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma, package.json, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-01-PLAN.md</read_first>
  <action>After schema changes are complete, run `npx prisma db push` so the live local database matches the new product model before verification. If Prisma requires non-TTY confirmation, rerun with `npx prisma db push --accept-data-loss` and record that choice in the summary. Then run the product catalog test and lint so this foundation cannot pass on generated types alone.</action>
  <verify>
    <automated>npx prisma db push && npm run test -- tests/inventory/product-catalog-sync.spec.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exits 0 after the product schema is added
    - the product catalog test passes after db push
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Product catalog persistence and sync are verified against the real local database schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->indexeddb | Product create/edit forms write untrusted user input into local cache and queue state. |
| client->product-sync-api | Background sync pushes queued product updates to authenticated server endpoints. |
| api->database | Product upsert endpoints convert client payloads into canonical catalog rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Tampering | src/features/inventory/hooks/use-product-catalog.ts | mitigate | Validate required fields, integer-only price range, and normalized payload shape before local persistence. |
| T-02.1-02 | Spoofing | src/app/api/inventory/products/route.ts | mitigate | Require authenticated session and bind product upserts to active user context. |
| T-02.1-03 | Repudiation | src/lib/offline/sync-queue.ts | mitigate | Keep product sync rows append-only with timestamps and entity ids for reconciliation traceability. |
| T-02.1-SC | Tampering | npm/pnpm installs | mitigate | No new package installs belong in this plan; if a dependency is introduced later, run package safety review first. |
</threat_model>

<verification>
- `npx prisma db push` succeeds after product schema changes
- `npm run test -- tests/inventory/product-catalog-sync.spec.ts` passes
- `npm run lint` passes
</verification>

<success_criteria>
- A local-first product catalog exists and can be updated without blocking on network
- Product prices are stored as integer IDR values within the locked range
- Product sync is separated from inventory mutation sync and test-covered
</success_criteria>

<output>
Create `.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-01-SUMMARY.md` when done
</output>
