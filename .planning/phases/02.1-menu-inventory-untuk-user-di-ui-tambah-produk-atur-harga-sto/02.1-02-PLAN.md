---
phase: 02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto
plan: "02"
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified: [src/app/app/layout.tsx, src/app/app/inventory/page.tsx, src/features/inventory/components/inventory-tabs.tsx, src/features/inventory/components/product-form-dialog.tsx, src/features/inventory/components/product-table.tsx, src/features/inventory/hooks/use-product-price-input.ts, tests/inventory/inventory-product-tab.spec.tsx, tests/inventory/product-price-input.spec.ts]
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Inventory UI is a single authenticated route with 3 tabs, not split pages or card-first catalog browsing (D-01, D-04)."
    - "Tambah produk uses a modal dialog and returns the operator to the same Produk tab with a reset form after success (D-02, D-03)."
    - "Harga jual accepts raw digits while typing, formats as IDR on blur, rejects decimals, and remains editable for future transactions only (D-05, D-06, D-07, D-08)."
  artifacts:
    - path: "src/app/app/inventory/page.tsx"
      provides: "Authenticated inventory route with tab shell"
      exports: ["default"]
    - path: "src/features/inventory/components/product-form-dialog.tsx"
      provides: "Tambah Produk modal and price-validation form behavior"
      exports: ["ProductFormDialog"]
    - path: "src/features/inventory/components/product-table.tsx"
      provides: "Compact searchable product list with edit action"
      exports: ["ProductTable"]
    - path: "src/features/inventory/hooks/use-product-price-input.ts"
      provides: "Raw-digit typing and IDR-on-blur formatting behavior"
      exports: ["useProductPriceInput"]
  key_links:
    - from: "src/app/app/layout.tsx"
      to: "src/app/app/inventory/page.tsx"
      via: "authenticated shell navigation link"
      pattern: "/app/inventory"
    - from: "src/features/inventory/components/product-form-dialog.tsx"
      to: "src/features/inventory/hooks/use-product-price-input.ts"
      via: "shared raw-input and blur-format handling"
      pattern: "useProductPriceInput"
    - from: "src/features/inventory/components/product-table.tsx"
      to: "src/features/inventory/hooks/use-product-catalog.ts"
      via: "local-first product list and update actions"
      pattern: "useProductCatalog"
---

<objective>
Deliver the inventory route and Produk tab UI: operators can open `/app/inventory`, search products in a compact table, create a product in a modal, and edit selling price using the locked IDR input rules.

Purpose: This turns the new product catalog foundation into a usable inventory management screen aligned with the approved UI contract.
Output: Inventory route shell, product tab UI, search table, create/edit modal, price formatting hook, and passing UI tests.
</objective>

<execution_context>
@C:/Users/lukas/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lukas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-01-SUMMARY.md
@src/app/app/layout.tsx
@src/app/app/page.tsx
@src/components/ui/dialog.tsx
@src/components/ui/table.tsx
@src/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing UI tests for inventory route, product modal flow, and price formatting</name>
  <files>tests/inventory/inventory-product-tab.spec.tsx, tests/inventory/product-price-input.spec.ts</files>
  <read_first>tests/sync/negative-stock-warning.spec.tsx, src/app/app/page.tsx, src/components/ui/dialog.tsx, src/components/ui/table.tsx, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md</read_first>
  <action>Create failing tests that assert `/app/inventory` renders tabs `Produk`, `Stock In`, and `Stock Adjustment`, that `Tambah Produk` opens a modal dialog and resets the form after successful save while staying on the `Produk` tab, and that raw price input `15000` formats to `Rp15.000` on blur with decimal input rejected. Keep assertions on exact tab labels and CTA copy from the UI-SPEC so later implementation cannot drift.</action>
  <verify>
    <automated>npm run test -- tests/inventory/inventory-product-tab.spec.tsx tests/inventory/product-price-input.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - both new test files exist and fail before implementation
    - one test asserts exact tab labels `Produk`, `Stock In`, `Stock Adjustment`
    - one test asserts raw-digit typing plus IDR formatting on blur
    - one test asserts the modal form resets after success without leaving `Produk`
  </acceptance_criteria>
  <done>UI behavior is locked in tests before building the inventory route and product tab.</done>
</task>

<task type="auto">
  <name>Task 2: Implement inventory route shell, Produk tab, and modal create/edit flow</name>
  <files>src/app/app/layout.tsx, src/app/app/inventory/page.tsx, src/features/inventory/components/inventory-tabs.tsx, src/features/inventory/components/product-form-dialog.tsx, src/features/inventory/components/product-table.tsx, src/features/inventory/hooks/use-product-price-input.ts</files>
  <read_first>src/app/app/layout.tsx, src/app/app/page.tsx, src/components/ui/dialog.tsx, src/components/ui/table.tsx, src/components/ui/input.tsx, src/components/ui/button.tsx, src/app/globals.css, src/features/inventory/hooks/use-product-catalog.ts, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md</read_first>
  <action>Add an `Inventori` navigation link in the authenticated shell and create `/app/inventory` as a single-page tabbed route. Implement the `Produk` tab with a compact searchable table showing `Nama produk`, `Harga jual`, `Stok saat ini`, and `Aksi`, plus a `Tambah Produk` modal dialog for create/edit. Add a reusable hook for raw-digit price entry that formats IDR on blur, enforces min `100`, max `999999999`, and blocks decimal values. Ensure success uses the locked CTA copy and leaves the user on the `Produk` tab with a cleared form after save.</action>
  <verify>
    <automated>npm run test -- tests/inventory/inventory-product-tab.spec.tsx tests/inventory/product-price-input.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/app/inventory/page.tsx` exists and renders the 3-tab shell
    - `src/app/app/layout.tsx` links to `/app/inventory`
    - `src/features/inventory/components/product-form-dialog.tsx` uses modal dialog interaction for create/edit
    - `src/features/inventory/components/product-table.tsx` exposes compact searchable product list behavior
    - the UI tests for route, modal, and price formatting pass
  </acceptance_criteria>
  <done>The inventory page and Produk tab match the approved UI contract and are usable on desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 3: Verify route-level product tab behavior and responsive-safe layout</name>
  <files>src/app/app/inventory/page.tsx, tests/inventory/inventory-product-tab.spec.tsx, tests/inventory/product-price-input.spec.ts</files>
  <read_first>src/app/app/inventory/page.tsx, src/app/globals.css, package.json, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-02-PLAN.md</read_first>
  <action>Run the targeted product-tab tests and lint, then manually confirm the inventory route keeps critical actions visible across mobile and desktop breakpoints as required by the UI-SPEC. If any layout issue appears, fix the route/components before sign-off rather than deferring to the next plan.</action>
  <verify>
    <automated>npm run test -- tests/inventory/inventory-product-tab.spec.tsx tests/inventory/product-price-input.spec.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - targeted product-tab tests pass
    - `npm run lint` exits 0
    - route shell keeps `Simpan`, `Ubah Harga`, and tab navigation accessible across breakpoints
  </acceptance_criteria>
  <done>The Produk tab is verifiably usable and aligned with the approved UI contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route->local catalog | The inventory page renders and mutates local-first product data from offline cache. |
| modal form->validation | Product create/edit modal receives untrusted text and numeric input from operators. |
| UI action->sync queue | Product saves schedule sync work that later crosses the network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04 | Tampering | src/features/inventory/hooks/use-product-price-input.ts | mitigate | Normalize raw digits, reject decimals, and clamp invalid prices before submit. |
| T-02.1-05 | Information Disclosure | src/app/app/inventory/page.tsx | mitigate | Keep the route behind the authenticated `/app` shell and reuse existing session-gated layout. |
| T-02.1-06 | Spoofing | src/features/inventory/components/product-form-dialog.tsx | mitigate | Bind saves to the local authenticated catalog hook rather than trusting arbitrary caller payloads. |
| T-02.1-SC | Tampering | registry/components | mitigate | Reuse existing local shadcn/base-ui primitives only; no third-party registry imports in this plan. |
</threat_model>

<verification>
- `npm run test -- tests/inventory/inventory-product-tab.spec.tsx tests/inventory/product-price-input.spec.ts` passes
- `npm run lint` passes
- `/app/inventory` renders the locked tab shell and product flow without navigation regressions
</verification>

<success_criteria>
- Operators can reach a dedicated inventory route from the app shell
- The Produk tab matches the locked modal/table/price-input behavior
- IDR input formatting and validation are test-covered and non-ambiguous
</success_criteria>

<output>
Create `.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-02-SUMMARY.md` when done
</output>
