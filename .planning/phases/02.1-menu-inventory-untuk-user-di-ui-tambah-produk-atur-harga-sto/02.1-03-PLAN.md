---
phase: 02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto
plan: "03"
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02"]
files_modified: [src/app/app/inventory/page.tsx, src/features/inventory/components/stock-in-tab.tsx, src/features/inventory/components/stock-adjustment-tab.tsx, src/features/inventory/components/stock-adjustment-preview.tsx, src/features/inventory/components/negative-stock-warning.tsx, src/features/inventory/hooks/use-stock-mutation.ts, tests/inventory/stock-adjustment-flow.spec.tsx, tests/inventory/inventory-tab-reset.spec.tsx]
autonomous: true
requirements: [INV-01, INV-03, SYNC-05, UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Stock In and Stock Adjustment are separate tabs, not a shared dropdown form (D-09)."
    - "Stock Adjustment uses absolute final stock input and shows computed delta before submit (D-10)."
    - "Negative stock warnings stay soft, use exact copy, allow `Enter` bypass, and reset the active tab form after success without navigation loss (D-11, D-12 plus inherited D-08 to D-11 from phase 2)."
  artifacts:
    - path: "src/features/inventory/components/stock-in-tab.tsx"
      provides: "Dedicated Stock In UI tab"
      exports: ["StockInTab"]
    - path: "src/features/inventory/components/stock-adjustment-tab.tsx"
      provides: "Dedicated Stock Adjustment UI with absolute-stock input"
      exports: ["StockAdjustmentTab"]
    - path: "src/features/inventory/components/stock-adjustment-preview.tsx"
      provides: "Computed delta preview for adjustment before save"
      exports: ["StockAdjustmentPreview"]
    - path: "src/features/inventory/hooks/use-stock-mutation.ts"
      provides: "Mutation submit behavior aligned with separate stock in/adjustment tabs"
      exports: ["useStockMutation", "persistStockMutation"]
  key_links:
    - from: "src/features/inventory/components/stock-adjustment-tab.tsx"
      to: "src/features/inventory/components/stock-adjustment-preview.tsx"
      via: "absolute-stock input drives delta preview"
      pattern: "StockAdjustmentPreview"
    - from: "src/features/inventory/components/stock-adjustment-tab.tsx"
      to: "src/features/inventory/components/negative-stock-warning.tsx"
      via: "soft warning gate before persisting negative stock result"
      pattern: "NegativeStockWarning"
    - from: "src/app/app/inventory/page.tsx"
      to: "src/features/inventory/components/stock-in-tab.tsx"
      via: "inventory route tab composition"
      pattern: "StockInTab"
---

<objective>
Deliver the `Stock In` and `Stock Adjustment` UI workflows on the inventory page: each action has its own tab, uses the product catalog foundation, and writes to the existing mutation queue with the locked warning and reset behavior.

Purpose: This exposes the phase-2 inventory engine through the approved operator-facing UI so stock updates can actually be performed from the browser.
Output: Dedicated stock tabs, absolute adjustment flow with computed delta preview, warning integration, success-reset behavior, and passing interaction tests.
</objective>

<execution_context>
@C:/Users/lukas/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lukas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-01-SUMMARY.md
@.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-02-SUMMARY.md
@src/features/inventory/hooks/use-stock-mutation.ts
@src/features/inventory/components/stock-mutation-form.tsx
@src/features/inventory/components/negative-stock-warning.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests for separate stock tabs, absolute adjustment input, and success reset behavior</name>
  <files>tests/inventory/stock-adjustment-flow.spec.tsx, tests/inventory/inventory-tab-reset.spec.tsx</files>
  <read_first>tests/sync/negative-stock-warning.spec.tsx, tests/inventory/mutation-queue.spec.ts, src/features/inventory/components/stock-mutation-form.tsx, src/features/inventory/components/negative-stock-warning.tsx, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md</read_first>
  <action>Create failing interaction tests that assert `Stock In` and `Stock Adjustment` render as separate tabs, that `Stock Adjustment` takes an absolute final-stock input and shows a computed `perubahan` preview before submit, and that successful submit resets the active form while staying in the same tab. Include an assertion that the negative-stock warning still uses the exact approved copy and `Enter` bypass path when projected stock becomes zero or negative.</action>
  <verify>
    <automated>npm run test -- tests/inventory/stock-adjustment-flow.spec.tsx tests/inventory/inventory-tab-reset.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - both new interaction tests exist and fail before implementation
    - one test asserts absolute-stock input drives delta preview text
    - one test asserts the active tab is unchanged after successful submit
    - one test asserts exact negative-stock warning copy remains unchanged
  </acceptance_criteria>
  <done>Separate stock tab behavior and reset rules are locked before UI refactoring begins.</done>
</task>

<task type="auto">
  <name>Task 2: Implement dedicated Stock In and Stock Adjustment tabs on top of existing mutation queue logic</name>
  <files>src/app/app/inventory/page.tsx, src/features/inventory/components/stock-in-tab.tsx, src/features/inventory/components/stock-adjustment-tab.tsx, src/features/inventory/components/stock-adjustment-preview.tsx, src/features/inventory/components/negative-stock-warning.tsx, src/features/inventory/hooks/use-stock-mutation.ts</files>
  <read_first>src/app/app/inventory/page.tsx, src/features/inventory/components/stock-mutation-form.tsx, src/features/inventory/components/negative-stock-warning.tsx, src/features/inventory/hooks/use-stock-mutation.ts, src/features/inventory/hooks/use-product-catalog.ts, src/lib/inventory/mutation-event.ts, .planning/phases/02-inventory-and-sync-engine/02-CONTEXT.md, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-CONTEXT.md, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-UI-SPEC.md</read_first>
  <action>Refactor the existing shared stock mutation form into two dedicated tab components. `Stock In` should accept product selection and positive quantity input only. `Stock Adjustment` should accept `stok akhir` as the primary input, compute the delta against current stock, and show a preview component before save. Keep the soft warning dialog with exact copy and `Enter` bypass behavior, but wire it from the separate stock tabs rather than a shared mutation-type dropdown. On successful submit, clear the current form, keep the operator on the same tab, and leave unsent drafts in other tabs untouched during the same session.</action>
  <verify>
    <automated>npm run test -- tests/inventory/stock-adjustment-flow.spec.tsx tests/inventory/inventory-tab-reset.spec.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/inventory/components/stock-in-tab.tsx` and `stock-adjustment-tab.tsx` both exist
    - `src/features/inventory/components/stock-adjustment-preview.tsx` shows computed delta from absolute final stock input
    - `src/features/inventory/hooks/use-stock-mutation.ts` no longer depends on a mutation-type dropdown in the UI
    - the interaction tests for separate tabs, preview, and reset behavior pass
  </acceptance_criteria>
  <done>The inventory page exposes stock updates through the locked tab-specific workflows instead of the earlier shared mutation form.</done>
</task>

<task type="auto">
  <name>Task 3: Verify inventory mutation tab UX, warning behavior, and lint health</name>
  <files>tests/inventory/stock-adjustment-flow.spec.tsx, tests/inventory/inventory-tab-reset.spec.tsx, src/app/app/inventory/page.tsx</files>
  <read_first>tests/inventory/stock-adjustment-flow.spec.tsx, tests/inventory/inventory-tab-reset.spec.tsx, tests/sync/negative-stock-warning.spec.tsx, package.json, .planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-03-PLAN.md</read_first>
  <action>Run the new inventory-tab tests plus the existing negative-stock warning test to ensure the UI refactor did not break inherited warning behavior from phase 2. Then run lint and confirm the page still satisfies the UI-SPEC responsiveness and action-visibility requirements before sign-off.</action>
  <verify>
    <automated>npm run test -- tests/inventory/stock-adjustment-flow.spec.tsx tests/inventory/inventory-tab-reset.spec.tsx tests/sync/negative-stock-warning.spec.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - the two new inventory-tab tests pass together with the existing negative-stock warning test
    - `npm run lint` exits 0
    - warning behavior remains non-blocking and `Enter` bypass stays intact after the tab refactor
  </acceptance_criteria>
  <done>The stock update workflows are verified as operator-usable and consistent with both phase 2 and phase 02.1 contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tab form->mutation hook | Stock forms transform operator input into queued mutation events. |
| warning dialog->commit path | The negative-stock warning mediates continuation into potentially unsafe stock states. |
| route->local catalog | Tab flows depend on cached product stock snapshots that may be stale until sync completes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-07 | Tampering | src/features/inventory/components/stock-adjustment-tab.tsx | mitigate | Compute delta from current stock plus absolute final stock input instead of trusting user-supplied delta values. |
| T-02.1-08 | Denial | src/features/inventory/components/negative-stock-warning.tsx | mitigate | Preserve non-blocking bypass path and keyboard continuation so warning does not halt cashier throughput. |
| T-02.1-09 | Repudiation | src/features/inventory/hooks/use-stock-mutation.ts | mitigate | Continue using the existing mutation builder and queue metadata contract from phase 2 for traceability. |
| T-02.1-SC | Tampering | UI refactor regressions | mitigate | Keep inherited warning tests in verification so the refactor cannot silently weaken protected behavior. |
</threat_model>

<verification>
- `npm run test -- tests/inventory/stock-adjustment-flow.spec.tsx tests/inventory/inventory-tab-reset.spec.tsx tests/sync/negative-stock-warning.spec.tsx` passes
- `npm run lint` passes
- stock tabs match the locked UX contract for separate tabs, absolute adjustment, and reset behavior
</verification>

<success_criteria>
- `Stock In` and `Stock Adjustment` are distinct workflows on the inventory page
- adjustment uses absolute final stock with a visible computed delta preview
- negative-stock warning behavior remains exact, soft, and keyboard-bypassable
</success_criteria>

<output>
Create `.planning/phases/02.1-menu-inventory-untuk-user-di-ui-tambah-produk-atur-harga-sto/02.1-03-SUMMARY.md` when done
</output>
